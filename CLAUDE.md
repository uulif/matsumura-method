# 指示

- 作業過程は見せずに結果だけ報告する
- 「見せて」と言われたら作業過程を見せる
- 日本語で返答する

# 展開仕様

テキストが長い場合に「続きを見る」で展開・編集できる共通パターン。

## 動作の仕組み
- `overflow: hidden` で超過分を隠す
- `checkOverflow()` で溢れを検知 → 「続きを見る」表示
- 展開時は `max-height: none` で全文表示

## 3段階フロー
1. 通常表示（省略）
2. 展開表示（閲覧モード）
3. 編集モード

## 高さ設定（場所により異なる）
- ホーム画面の目標タイトル・進捗詳細: 113px（約4〜5行）
- 人生設計カード: 158px（約6〜7行）
- 年齢別目標テキストエリア: 2行分
