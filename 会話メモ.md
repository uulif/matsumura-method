# 会話メモ

「メモに残して」と言われた時だけ、日時と内容を追記する。

---

### ノートビュー設計（参照画像: OneDrive\画像\Screenshots\スクリーンショット 2026-02-19 223254.png）

**目的**: Galaxy Fold展開時・iPad・PC等の横幅が広いデバイス用レイアウト。通常スマホ用のGTDタブとは別物。

**レイアウト**: 左右2カラム同時表示（タブ切替ではない）
- 左カラム: 「今日」ステータス別（未着手/進行中/完了）
- 右カラム: 「整理用」GTDカテゴリ別（INBOX/次に取/カレンダー/プロジェクト/待ち/ウィッシュ）

**デザインの核心（Notion風 "机の上" 感）**:
- カラム間に枠線・ボーダーなし。余白(gap)だけで自然に分離
- グループ同士もボーダーなし。余白で分離
- 行間だけ薄い border-bottom
- ボックスシャドウなし
- 左右カラムは高さが独立（揃えない）
- 全体的に空気感のある余白。詰まらない
- 「お菓子の箱に入れる」のではなく「机の上に紙を並べた」感覚

### 2026-02-19 セッションでやったこと

#### ① 全体5WR → 致命的バグ一括修正
5人のAIレビュアー（UX/UI、アーキテクチャ、データ設計、機能完整性、セキュリティ）で全体レビュー実施。以下の致命的・重要バグを修正：

| コミット | 内容 |
|---|---|
| `858453f` | AI補足対応のデータ構造ファクトリ導入（createTaskData等） |
| `da1b718` | XSSエスケープ漏れ修正（pages.js + app.js 28箇所） |
| `3e3e661` | APIキーをソースコードから除去→設定画面に移行 |
| `6b4ad48` | showToast二重定義を統一 |
| `f7c959e` | GTDタブのプロパティ名不一致、ストア名修正、hasJournalData修正、削除確認追加、alert→showToast統一 |
| `f596ee3` | escapeHtml追加漏れ約25箇所を修正（リグレッションレビューで検出） |
| `d7c445c` | メモ欄・完了チェック・ダークモード修正を実装 |

#### ② 3ステップ5WR
メモ欄・完了チェック・ダークモードの3機能について5人レビュー実施。
推奨実装順序: メモ欄 → 完了チェック → ダークモード

#### ③ 3ステップ実装

**メモ欄:**
- タスク・ルーティンの追加/編集モーダルにメモ欄を追加
- 追加モーダルでは`<details>`で折りたたみ表示
- 一覧でメモ冒頭40文字をプレビュー
- `task.memo` → `task.notes` に統一
- GTDTab・ListPage両方のレンダリングを更新

**完了チェック:**
- タスクアイテムにチェックボックス追加（44x44pxタッチターゲット）
- `toggleTaskStatus()` でopen/done切り替え
- 完了タスクは取り消し線+半透明
- タブカウントから完了タスクを除外

**ダークモード修正:**
- `body.classList.toggle('dark-mode')` のJSロジック追加（init + toggleSetting）
- `body.dark-mode` でCSS変数8個を再定義
- `:root`に不足変数(`--bg-light`, `--bg-secondary`)を追加
- 主要コンポーネントの白背景をCSS変数に置換（モーダル全種、ナビバー、ヘッダー等）
- ボーダー・テキスト色のハードコードをCSS変数に置換

#### ④ ノートビュー実装（2カラム並列表示）
Notion風GTDテンプレートを参考にした、ワイドスクリーン専用の2カラムレイアウト。

**実装内容:**
- noteview.js / noteview.css を新規作成
- 左「今日」（ステータス別）+ 右「整理用」（GTDカテゴリ別）同時表示
- 3ステートタスクステータス（open→in_progress→done→open）
- GTDタブにもin_progress対応を追加（pages.js + style.css）
- 5WR 3回実施、全指摘対応済み

**コミット履歴:**
| コミット | 内容 |
|---|---|
| `0bf7352` | ノートビュー全面リライト |
| `3fb8354` | 5WR第1回修正（GTDタブin_progress対応等） |
| `245a7fe` | 2カラム並列表示に再設計 |
| `53f7a58` | 5WR第2回修正（タッチターゲット・バッジ重複除去等） |
| `4ef4de2` | 5WR第3回修正（＋新規制御・F-BOXアイコン・escapeHtml強化等） |

**導線:** GTDタブからタップで切り替え（現状のまま）

### 残作業（ノートビュー改善・未実装）
- 行線の視認性向上（border色をもう少し濃くする等）
- グループヘッダーの存在感を上げる
- ルーティンは義務・維持カテゴリのみに絞る（目標・指針・候補は除外）

### コードベース監査（2026-02-20）

UI起点のコード追跡調査を実施。全画面→全onclick→全メソッドを精密に追跡。

#### 修正済みバグ

| コミット | 内容 |
|---|---|
| `376089b` | GTDタブの致命的バグ6件修正（deleteメソッド名不一致4件、存在しないonclick2件） |
| `2aef1b8` | 設定読み込み漏れ8件、SWキャッシュ漏れ2件、アイコンパス不整合1件 |

**GTDタブバグ詳細（pages.js）:**
- `deleteFirstBoxItem` → `deleteFirstBoxItemById`
- `deleteTask` → `deleteTaskById`
- `deleteRoutine` → `deleteRoutineById`
- `deleteMaterial` → `deleteMaterialById`
- `openTaskDetail`（存在しない）→ `showEditTaskModal`
- `openRoutineDetail`（存在しない）→ `showEditRoutineModal`

**設定読み込み漏れ（app.js loadAllData）:**
themeApplyAll, font, transition, detailBtnStyle, detailBtnColor, inputModalType, scheduleWidgetStyle, routineWidgetStyle の8件を追加

#### デッドコード一覧（未削除・要判断）

**app.js デッドメソッド 12件（+関連1件）:**
| 行 | メソッド | 理由 |
|---|---|---|
| 2768 | `toggleRoutineDetail` | toggleRoutineCardに置換済み |
| 3579 | `setPattern` | 呼び出すUIなし |
| 3595 | `toggleQuickFieldset` | 呼び出すUIなし |
| 3735 | `toggleRoutineFromWidget` | toggleRoutineに統合済み |
| 3764 | `updateScheduleSlot` | パターン方式移行後の残骸 |
| 3893 | `showTodayTaskModal`（+closeTodayTaskModal, saveTodayTask） | 呼び出すUIなし |
| 3980 | `switchTodayPattern` | showPatternSelectModalに代替 |
| 4808 | `showFullText` | 展開UI方式に移行後の残骸 |
| 5423 | `showRoutineEditStyleModal`（+selectRoutineEditStyle） | 呼び出すUIなし |
| 5549 | `showRoutineTooltip` | 呼び出すUIなし |
| 5653 | `toggleAllHomeRoutineCards` | 呼び出すUIなし |
| 5672 | `toggleRoutineCoreDetail` | 呼び出すUIなし |
| 6252 | `exportMemoAsImage` | 呼び出すUIなし |

**showModal系デッドコード（app.js L5849-7264、約1,400行）:**
showModal()を呼び出すUIボタンがどこにも存在しない。以下が全てデッド:
- showModal / closeQuickModal（クイックモーダル本体）
- saveMoney / saveCalorie（家計簿・カロリー記録）
- toggleTimer / startTimer / stopTimer / updateTimerDisplay / saveTimer（タイマー）
- saveMemo（クイックメモ入力）※表示側のdeleteQuickMemo/undoDeleteMemoは使用中
- toggleAttachMenu / attachFile / openFilePicker / openCamera 等（添付ファイル系）
- openDrawingCanvas ほか描画系全メソッド（お絵描きキャンバス約816行）
- saveScheduleRecord（スケジュール記録）
- toggleFavorites / renderFavoritesList 等（お気に入り系）

**db.js デッド関数 11件:**
getTodayJournal, getCurrentMonthlyGoal, saveDailyData, getDailyData, getTodayDailyData, calculateMonthlyAverageRate, getTask, getTasksByType, getRoutine, getRoutinesByType, getMaterial

**db.js デッドストア 1件:** `dailyData`

**CSS未使用セクション（style.css、約495行削除可能）:**
- now-doing / now-recommend / recommendation 関連（L6552-6649、約100行）
- .loading / .spinner（L4016-4034、約20行）
- routine-table（L4785-4843、約60行）
- routine-edit-card（L4645-4756、約110行）
- routine-twoline（L5081-5142、約60行）
- routine-tags（L5144-5213、約70行）
- routine-tooltip（L5216-5293、約80行）

**CSS重複定義 16箇所:**
重大なもの: .action-row, .add-btn, .swipe-container, .task-item, .routine-item が全く異なるレイアウトで2回定義。後の定義で上書き。

#### デッドコード削除（2026-02-20 実施完了）

| コミット | 内容 | 削除行数 |
|---|---|---|
| `2ab1bcc` | app.js デッドメソッド12件を削除 | -282行 |
| `17c26bb` | app.js showModal系デッドコード（家計簿・カロリー・タイマー・メモ入力・添付・描画キャンバス・スケジュール記録・お気に入り）を削除 | -1,471行 |
| `c8c9007` | db.js デッド関数11件+補足データセクションを削除 | -77行 |
| `827678c` | style.css 未使用セクション7種を削除 | -522行 |
| **合計** | | **-2,352行** |

**保持したもの:**
- `deleteQuickMemo` / `undoDeleteMemo`（pages.js日誌表示で使用中）
- `closeModal`（設定モーダルで使用中）
- `dailyData`ストア定義（既存DB互換性のため）

#### 残課題

1. **CSS重複解消**: 16箇所の重複定義。重大5件（.action-row, .add-btn, .swipe-container, .task-item, .routine-item）は同クラス名で異レイアウトの設計衝突。クラス名リネームで対応が必要
2. **apple-touch-icon**: SVGはiOSで非対応。PNG版アイコンの作成が必要
3. **不要ファイル**: プロジェクトルートの`nul`ファイル（ping誤出力）

### 残作業（ダークモード関連）
ダークモードレビューで検出されたPhase 2以降の残作業：
- `background: white/#fff/#ffffff` が約80箇所残存 → `var(--bg-main)`等に置換
- `color: #000/#333` が約30箇所残存 → `var(--text-primary)`等に置換
- テーマカラー×ダークモードの組み合わせ未考慮
- 個別コンポーネント（日誌、月次目標、人生設計、マニュアル等）の細かいダークモード対応

### ファイル構成（主要）
| ファイル | 行数 | 役割 |
|---|---|---|
| js/app.js | ~9400行 | メインアプリオブジェクト |
| js/pages.js | ~4300行 | ページレンダリング関数 |
| js/db.js | ~700行 | IndexedDB操作 |
| js/noteview.js | ~780行 | ノートビュー（サイドバー+テーブル型ダッシュボード） |
| js/icons.js | - | SVGアイコン定義 |
| css/style.css | ~12000行 | 全スタイル |
| css/noteview.css | ~600行 | ノートビュー専用CSS |

### 重要な設計ノート
- **2つのレンダリングシステム**: GTDタブ（renderGTDTaskTab等）とリストページ（renderTaskListPage等）の2系統がある。UIの変更は両方に適用が必要
- **render()は全体再描画**: innerHTML全置換のため、インライン編集は不可→モーダルで編集
- **ファクトリ関数**: createTaskData / createRoutineData / createMaterialData（db.js）でAI対応データ構造を統一生成
- **escapeHtml()**: pages.js L7で定義。ユーザー入力をHTMLに埋め込む全箇所で使用必須

---

## 2026-02-21 セッションメモ

### やりたいことリスト

#### 大：外部連携
1. Notion自動バックアップ（API経由、無料）
2. Googleカレンダー連携（API経由、OAuth認証必要、無料）

#### 中：機能追加
3. 達成率グラフ表示（1WEEK・1MONTH）※ロジック実装済み、UIのみ
4. 月次振り返りの5択判断UI（継続・強化・改善・縮小・廃止）
5. タイマー機能（作業時間→日誌へ自動反映）
6. 週次・月次の見直し通知
7. 月次目標のルーティン→日誌への自動反映
8. PC対応（max-width上限撤廃 + レスポンシブ、スマホUIは維持）

#### 小：項目追加
9. ポリシー点数2項目（各10点満点）
10. 達成時の報酬確認（月次目標STEP3）
11. 今日の意気込み欄

#### ドキュメント
12. 使い方マニュアル作成（AIプロジェクトの使い方も含める）

#### 将来（今はやらない）
13. アプリ内AI完結（商品化・有料化時に検討）

### 設計方針メモ
- **アプリ = 実行と記録** / **AI = 思考と計画** で役割分担
- AIで目標を立てる時はNotionバックアップデータ + 事前プロンプトを用意したAIプロジェクトを使う
- Notionはバックアップ兼閲覧用ダッシュボード（データをそのまま表やページで見れる）
- GoogleドライブよりNotionの方が適している（容量無制限・検索可能・データがそのまま読める）

### ガイドとアプリの照合結果（主要な不一致）
- ポリシー点数2項目（各10点満点）→ 未実装 → やりたいことリスト#9
- 達成時の報酬確認（STEP3）→ 未実装 → やりたいことリスト#10
- 達成率グラフ（1WEEK・1MONTH）→ ロジックあり、UI準備中 → やりたいことリスト#3
- 月次振り返り5択判断UI → 未実装 → やりたいことリスト#4
- 今日の意気込み欄 → 未実装 → やりたいことリスト#11
- GAS連携・PDF化・ドライブ保存 → Notion連携で代替

---

## 2025-02-12 セッションメモ

### やったこと（前セッション以前）

#### 第1段階（完了済み）
- タブバー6個化（ホーム・目標一覧・ルーティン・タスク・資料・設定）
- F・BOX振り分けフロー実装

#### テキスト修正
- 「振り分け」→「F・BOX」にリネーム
- 振り分けフローの質問文・選択肢を全面改善

#### F・BOX 2パターン実装
- パターンA: ホームに3ボタン（日誌・F・BOX一覧・振り分け）
- パターンB（デフォルト）: F・BOXを開くと入力欄＋「とりあえず入れる」「そのまま振り分ける」

### ボックス割り当て（確定）
| ページ | ボックス |
|---|---|
| タスク | アクションリスト、プロジェクト、待機リスト、カレンダー、ウィッシュ |
| ルーティン | 目標、義務、維持、指針、候補 |
| 資料 | 資料保管 |

### ローカルサーバー
- browser-sync使用、ポート3003、`--listen 0.0.0.0`でLAN公開
- 起動コマンド: `npx browser-sync start --server --files "**/*" --port 3003 --no-open --listen 0.0.0.0`

---

## 2026-02-22〜24 セッションメモ（複数セッション分）

### 54b826d → 73c93cd の主要変更（30コミット）

#### カレンダー関連（大量改修）
- カレンダーを振り返りから独立ページ化
- セル高さ拡大・ドラムピッカー・予定追加モーダル
- QAサイクル2回実施（致命的バグ3件修正含む）

#### スワイプナビゲーション
- 全11ページをフラット化してスワイプ順次移動対応
- セクション境界で最初のタブに遷移する制御追加

#### ホーム画面
- カード余白のレスポンシブ化

#### v257〜v260（カレンダー改善+UI刷新基盤）
- v257: カレンダー改善（塗りつぶし削除・サマリー削除・動的行数）
- v258: カレンダー改善（ボーダー一致・タップトグルサマリー）+ QA修正
- v259: すぐやる/アクションリスト分離、ヘルプアイコン、F-BOX改善
- v260: UIスタイルテーマシステム実装（角丸207箇所変数化、3テーマ: minimal/soft/vivid）

### UI刷新設計（2026-02-24セッション）

前回セッションでUI刷新の全体計画を策定。詳細は `UI刷新設計書.md` に記載。

**概要:**
- 7チーム構成（統括・トークン基盤・部品・画面UX・操作動き・書く体験・品質保証）
- 4Phase実行順序（Phase 1→2→3→4、各Phase内は並列あり）
- 9セーフガード（A〜I）で品質担保
- 参照アプリ: Todoist・Notion・Apple リマインダー

**エージェント実行:** 5つの設計エージェント（トークン基盤・部品統一・画面UX・操作動き・書く体験）を起動し全て完了したが、Context Limitでセッションが落ち、結果が消失。

---

## 2026-02-25 セッションメモ

### セッション経緯
1. 前回セッション（2026-02-24）がContext Limitで落ちた状態から開始
2. UI刷新設計書にはPhase 0のみ残存、Phase 1〜4の設計結果は消失
3. 新セッションのClaude側に前回の計画詳細（7チーム・4Phase・9セーフガード）の記憶なし
4. ユーザーが前回セッションの全文を貼り付けて復元
5. 情報の差分を確認 → 不足（Phase数の誤認等）はあったが矛盾はなし
6. 会話メモ.md と UI刷新設計書.md を更新して次回に備える

### 教訓
- Context Limitで落ちた場合、設計書に書き切れていない情報は消失する
- 計画段階の成果物（チーム構成・セーフガード等）は設計書に即座に書き込むべき
- セッション復元時は前回の全文が必要になる場合がある

### このセッションでやったこと
1. 前回消失分の復元（チーム構成・Phase・セーフガードを設計書に記載）
2. クラッシュ対策の設計（ファイル書き込み義務、順次実行、git commit等7項目）
3. コード全域調査（4エージェント並列）→ 正確な数値を取得
4. 5エージェント分の精密プロンプトを設計書に全文記載
5. エージェント実行手順を設計書に記載（新セッション向け）

---

## 2026-03-02 QAサイクル実行記録

### 対象変更
?ボタン削除→長押しヘルプ化、月次ページ遷移アニメーション除去、タブタッチ領域拡大

### Phase 1: コードレビュー（3ラウンド）
- **テーマ**: ロジック正当性、FIELD_HELP網羅性、リグレッション、UI/UX、エッジケース（5エージェント）
- **Round 1**: 7件修正（touch→mouse二重発火、navigate条件漏れ、F・BOXキー不一致、ポップアップコピー不可、animationendフォールバック、routine-today helpKey、touchstartガード）
- **Round 2**: 3件修正（life-list除外削除、-webkit-touch-callout設定、_FBOX_HELP定数化）
- **Round 3**: 0件 → Phase 1完了

### Phase 2: 実施テスト（Playwright）
- **テスト項目**: 18項目（Phase1修正確認5、主要操作8、エッジケース2、セキュリティ1、追加2）
- **結果**: 2回連続 18/18 全通過
- **コミット**: `2535cd6`

### 修正件数
- 修正済み: 10件 / 未修正: 0件

### 次回引き継ぎ
- ヘルプテキスト追加（~50+箇所）が未着手
- 日誌ページの旧式ヘルプボタン（5箇所）変換が未着手

---

## 2026-03-03 セッションメモ

### 実装内容（v282〜v288、8コミット）

#### UI変更・機能追加

| コミット | バージョン | 内容 |
|---|---|---|
| `1a46c28` | v282 | 完了誤タップ対策：Undoトースト（5秒間「元に戻す」ボタン表示） |
| `97d5039` | v283 | Pattern A削除（A/Bトグル・旧コード約91行除去）+ ノートビューアイコン変更（サイドバー+リスト型） |
| `cdaea24` | v284 | ダッシュボード日付限定（すぐやる/日付なしタスク除外）+ 完了タスク削除ボタン |
| `de43435` | v285 | チェックボックス簡略化（3状態→1タップ即完了+Undoトースト）+ ホーム青バー削除 |
| `3359d52` | v286 | テーブルソート統一（期限→種別）+ 期限数字拡大（nvb-dl-num）+ アイコン再変更 + タスク枠線均一化 |
| `78e3410` | v287 | ノートビューチェックボックス化（テキストボタン→丸型）+ サブタスク機能（parentId・インデント表示・追加モーダル） |
| `ddad718` | v288 | QA Phase 1修正（親削除→子昇格、orphan表示、XSSホワイトリスト、列数統一、parseIntガード等6件） |
| `e0e62d8` | - | QA Phase 2テスト追加（checkbox-subtask 23項目 + pattern-b修正4件） |

#### 主要な設計変更

- **チェックボックス**: 3状態サイクル（open→in_progress→done→open）を廃止。1タップで即完了+5秒Undoトースト。in_progressはノートビュー▶開始ボタンか編集モーダルでのみ設定可能
- **ダッシュボード**: 日付ありタスクのみ表示（緊急/注意/余裕の3カテゴリ）。日付なし・すぐやるタイプは種別フィルタで確認
- **サブタスク**: `parentId`フィールド追加。GTDタブ（インデント+border-left）とノートビュー（└記号+padding）で表示。親削除時は子をnullに昇格
- **Pattern A完全削除**: ノートビューは直接ダッシュボード表示。setNoteViewPattern・noteViewPattern状態も削除

### QAサイクル実行記録

#### Phase 1: コードレビュー（2ラウンド + バックグラウンド6エージェント）

**テーマ**: ロジック正当性、XSS・セキュリティ、エッジケース、UI/UX（4テーマ）

**Round 1 修正（6件）:**
1. 親タスク削除→子タスクのparentIdをnullに昇格（孤児防止）
2. フィルタ外の親を持つ子タスクをorphans配列で末尾表示
3. `_addSubtaskParentId`をcloseModalDirectでクリーンアップ
4. type・prevStatusのXSSホワイトリスト検証
5. テーブルtheadとtbodyの列数を5列に統一
6. parseInt+isNaNガード追加

**Round 2**: 全6修正の動作確認完了。新規の「確実に治せる」問題なし

**保留事項（設計変更レベル）:**
- 循環参照防止（通常操作では発生不可）
- 親完了時の子タスク連動
- pages.jsのorphan表示一貫性（noteview.jsは対応済み）
- pages.jsの~16箇所のID parseInt未適用（低リスク）
- F-BOX IDとタスクIDの衝突（理論上のみ）

#### Phase 2: 実施テスト（Playwright）

**テスト項目**: 56項目（3ファイル）
| ファイル | 項目数 | 内容 |
|---|---|---|
| checkbox-subtask.test.js（新規） | 23 | 完了/戻し、Undoトースト、削除、サブタスク作成/昇格/モーダル、表示（チェックボックス・インデント・ソート・列数・日付フィルタ）、XSS、Pattern A削除確認 |
| pattern-b.test.js（4件更新） | 15 | ダッシュボード表示、サイドバー、カード、フィルタ、日付計算 |
| qa-phase2.test.js（既存） | 18 | ヘルプ・操作系、リグレッション確認 |

**結果**: 56/56 全pass × 2回連続確認

#### 修正件数
- 修正済み: 6件 / 未修正: 0件（保留は全て設計変更レベル）

### 次回引き継ぎ
- ヘルプテキスト追加（~50+箇所）が未着手
- 日誌ページの旧式ヘルプボタン（5箇所）変換が未着手
- 実機テスト（スマホでの操作確認）が未実施
- 保留事項は実機テスト・利用状況を見てから判断

---

## 2026-03-11 セッションメモ

### 実装内容（v291→v293、3コミット）

| コミット | バージョン | 内容 |
|---|---|---|
| `75979bc` | v292 | スケジュール詳細機能：ホームの個別予定タップ→詳細モーダル、メモ欄追加、4スタイル対応 |
| `e217597` | v293 | UX改善：予定タップ時のページ遷移を廃止（ホーム上でモーダル表示）、ワンタイムタスクから4コア（前準備・反射条件・最低限・マニュアル）を除去 |

### 方針転換：インフラ整備を最優先に

ユーザーが実際にアプリを使い始めるにあたり、以下の問題が浮上：

- ローカルサーバー依存（同一WiFi限定）で外出先から使えない
- サーバー不安定（何度も再起動が必要だった）
- データバックアップ手段がゼロ（IndexedDBのみ）
- 更新のたびにキャッシュクリアが必要

→ **機能追加は一旦止め、インフラ整備を先にやる**ことで合意

優先順：
1. GitHub Pagesデプロイ
2. データエクスポート・インポート
3. デモデータクリア
4. 更新時のデータ移行テスト

### コードベース全体監査（2026-03-11）

デプロイ前に全ファイル（約13,000行）を4エージェント並列で完全監査。

#### 致命的（4件）
| # | 問題 | 場所 | 内容 |
|---|---|---|---|
| 1 | 保存エラーハンドリングなし | app.js 70箇所以上 | async関数にtry-catchなし。DB保存失敗してもユーザーに通知されない |
| 2 | SW無通知リロード | app.js registerServiceWorker | controllerchangeで入力中でも強制リロード。データ消失リスク |
| 3 | PWAアイコン不足 | manifest.json | 512x512アイコンがない。ホーム画面追加・PWAインストールが動作しない可能性 |
| 4 | renderTasksPageクラッシュ | pages.js renderTasksPage | todayJournal.routinesがundefinedの場合にmap()で即死（`|| []`フォールバックなし） |

#### 重要（8件）
| # | 問題 | 場所 | 内容 |
|---|---|---|---|
| 5 | loadAllData再呼び出し | app.js loadAllData | 再呼び出し時に未保存のメモリデータが上書き消失 |
| 6 | seedAllData再実行 | app.js init | ガードがtasks.length===0のみ。タスクだけ全削除すると他データ（ルーティン・日誌等）が重複投入される |
| 7 | DBスキーマ変更不可 | db.js createStoresIfNeeded | DB_VERSION上げても既存ストアの構造変更（インデックス追加等）ができない |
| 8 | saveDayMemo二重管理 | app.js saveDayMemo | journals + memos両方に保存。不整合リスク |
| 9 | モーダル二重表示 | app.js 7箇所 | insertAdjacentHTML系モーダルに既存チェックなし。連打で重複する |
| 10 | folderアイコン未定義 | icons.js | プロジェクトタブで使用する`folder`がgetIconに未定義（空文字返却） |
| 11 | manifest SVGサイズ | manifest.json | SVGのsizesが"192x192"→"any"が正しい |
| 12 | seed-data scope不一致 | seed-data.js | scope値が日本語('個人'/'社会')だがnoteviewは英語('personal'/'social')を期待。バッジ非表示 |

#### 軽微（6件）
| # | 問題 | 場所 | 内容 |
|---|---|---|---|
| 13 | escapeHtml未適用 | pages.js | メディア添付等の一部箇所 |
| 14 | startMinute未使用 | pages.js blocks | blocksスタイルでstartMinuteが無視される |
| 15 | gantt空表示 | pages.js gantt | スケジュール空の場合のフォールバックなし |
| 16 | デッドメソッド | app.js showDragTooltip | showDragOverlayに置換済みの残骸 |
| 17 | manifest不足フィールド | manifest.json | id/scope/lang未設定 |
| 18 | ハードコード日付 | seed-data.js | カレンダー・待機の日付が固定。時間経過でデモとして機能しなくなる |

#### 修正優先順
1→2→4→6→3→9→残り の順で修正予定

### 監査修正 第1弾：致命的4件（QAサイクル完了）

#### 修正コミット
| コミット | 内容 |
|---|---|
| `df0bfcd` | v294: 致命的4件修正（グローバルエラーハンドラー・SW更新通知・manifest.json・routinesクラッシュ防止） |
| `3fb7863` | QA Phase1 Round1: routines.length漏れ・初回訪問誤通知防止・CSS重なり・デバウンス・SW相対パス（6件） |
| `5d89916` | QA Phase2: テスト15項目追加 + getCurrentIdealActionクラッシュ防止 |

#### QAサイクル記録
**Phase 1: コードレビュー（2ラウンド）**
- Round 1: 6件修正（pages.js routines.length漏れ、hadControllerフラグ追加、z-index 9000化、ボタン44px化、3秒デバウンス、SW相対パス）
- Round 2: 全修正確認完了、新規問題なし

**Phase 2: Playwrightテスト**
- テスト項目: C01〜C15（15項目）
- 結果: 72/72 全pass × 2回連続（既存57 + 新規15）

### 監査修正 第2弾：重要4件（QAサイクル完了）

#### 対象
- #6 seedAllData再実行防止
- #9 モーダル二重表示防止（7箇所）
- #10 folderアイコン未定義
- #12 seed-data scope不一致（日本語→英語）

#### 除外
- #8 saveDayMemo二重管理 → **誤検出**。調査の結果、saveDayMemoはjournals.calendarMemoのみに保存。memosストアはクイックメモ用で別機能。二重管理ではない

#### 修正コミット
| コミット | 内容 |
|---|---|
| `06b5fba` | 重要4件修正（seedガード強化・モーダル二重防止・folderアイコン・scope修正） |
| `46eee05` | QA Phase1 Round1: getDataByKey→getSetting修正（未定義関数の致命的バグ） |
| `08dbef7` | QA Phase2: テスト7項目追加（C16〜C22、合計22項目） |

#### QAサイクル記録
**Phase 1: コードレビュー（2ラウンド）**
- Round 1: 1件修正（致命的 — `getDataByKey`関数が存在しない。`getSetting`に修正。try-catch内で無音失敗していたためseedガードが完全に機能していなかった）
- Round 2: 修正確認完了、新規問題なし

**Phase 2: Playwrightテスト**
- テスト項目: C16〜C22（7項目追加、合計22項目）
- 結果: 79/79 全pass × 2回連続（既存57 + 新規22）

### 監査修正 第3弾：軽微バグ5件修正

#### 修正対象
- #13 escapeHtml: 添付ファイル（画像/音声/動画/リンク/位置情報）のXSSバリデーション強化
- #14 startMinute: blocks/simpleスタイルで`startMinute`が反映されていなかった
- #15 ganttスタイル空配列 → **誤検出**（305行目でガード済み）
- #16 デッドコード: showDragTooltip/hideDragTooltip削除（app.js + style.css）
- #18 seed-dataハードコード日付: seedDate/seedMonth/seedYM関数で動的オフセット計算

#### 除外
- #5 loadAllData再呼び出し → 設計変更レベル
- #7 DBスキーマ変更不可 → 設計変更レベル

#### 修正コミット
| コミット | 内容 |
|---|---|
| `e20de72` | 軽微バグ5件修正（v295） |
| `72c7b4d` | QA Phase1 Round1: XSSバリデーション強化・デッドコード完全削除 |
| `8c4bab0` | QA Phase1 Round2: 緯度経度の範囲検証追加 |
| `d1c67e4` | QA Phase2: テスト7項目追加（C23〜C29、合計29項目） |

#### QAサイクル記録
**Phase 1: コードレビュー（2ラウンド）**
- Round 1: 6件修正（data URL src属性のescapeHtml追加、lat/lngのparseFloat検証、MIMEタイプ厳密化、target="_blank"にrel属性追加、dragNav.tooltip初期化削除、.drag-tooltip CSS削除）
- Round 2: 1件修正（lat/lngの範囲検証 -90〜90, -180〜180追加）

**Phase 2: Playwrightテスト**
- テスト項目: C23〜C29（7項目追加、合計29項目）
- 結果: 29/29 全pass × 2回連続

### 監査ステータスまとめ（最終）

| 重大度 | 件数 | 修正済み | 残り | 備考 |
|---|---|---|---|---|
| 致命的 | 4 | 4 | 0 | 完了 |
| 重要 | 8 | 4 (+1誤検出) | 3 | #5 loadAllData, #7 DBスキーマ, #11（→#3で修正済み） |
| 軽微 | 6 | 5 (+1誤検出) | 0 | 完了（#15は誤検出、#17は#3で修正済み） |

**残り重要2件（#5, #7）はどちらも設計変更レベルで、デプロイをブロックする問題ではない**

### 監査修正 第4弾：新規フルコードベース監査（2026-03-11）

4テーマ（DB整合性・非同期/状態管理・レンダリングエッジケース・パフォーマンス）で新規監査実施。

#### 修正対象（18件 + QAで5件追加 = 計23件修正）
**重要10件:**
- N1-N2: XSS escapeHtml漏れ（settings.name, journal.title）
- N3-N4: DB保存漏れ（saveLongtermCardExpand, saveMilestoneExpand）
- N5: await漏れ（completeFirstBoxSort 2箇所）
- N6: todayJournal nullクラッシュ（renderTasksPage/renderJournalPage/renderJournalSupplementPage）
- N7: Date NaN表示（deadline/dateTime, nextDate, createdAt）
- N8: スコア0 falsy判定（日誌・日誌一覧）
- N9: initRippleEffectsリスナー重複蓄積
- N10: モーダルDOMリーク（navigate時クリーンアップ）

**軽微8件:**
- N14: deleteData resolveタイミング統一
- N15: 読み取り関数 transaction.onabort追加
- N16-N17: toggleSchedule/updateMilestone/updateMonthlyRoutine nullガード
- N18: escapeHtml(0) falsy修正
- N19: timeAgo Date NaNチェック
- N20: escapeHtml→substring順序修正（5箇所）
- N21: todayJournal デフォルト値統一

**QA Phase1で追加修正5件:**
- R1: escapeHtml+substring修正漏れ（m.content）
- R2: ルーティン編集モーダルescapeHtml漏れ（6フィールド）
- R3: パターン選択モーダルescapeHtml漏れ
- R4: hasJournalData score不整合修正
- R5: navigate時field-help-overlay残存修正

#### 設計変更レベル（スキップ3件）
- N11: save/load競合（排他制御必要）
- N12: インポートのバッチ保存（新関数設計必要）
- N13: initDBFallbackストア不足（フォールバック再設計必要）

#### 修正コミット
| コミット | 内容 |
|---|---|
| `bc981c8` | 新規監査18件修正（v296） |
| `47f5878` | QA Phase1 Round1: escapeHtml漏れ8箇所・substring順序・score判定・overlay残存 |
| `6905572` | QA Phase2: テスト13項目追加（C30-C42、合計42項目）+todayJournalガード強化 |

#### QAサイクル記録
**Phase 1: コードレビュー（2ラウンド）**
- テーマ: XSS/セキュリティ + ロジック正当性 + リグレッション
- Round 1: 5件修正（escapeHtml漏れ8箇所、substring順序1箇所、hasJournalData score、field-help-overlay）
- Round 2: 確認完了、新規問題なし

**Phase 2: Playwrightテスト**
- テスト項目: C30〜C42（13項目追加、合計42項目）
- 結果: 42/42 全pass × 2回連続

---

### インフラ整備（2026-03-11）

#### 完了した4項目

| # | タスク | コミット | 内容 |
|---|---|---|---|
| 1 | GitHub Pagesデプロイ | `1279348`, `7f16d80` | SWパス相対化(v297)、manifest.json id修正、.nojekyll追加、Pages設定変更 |
| 2 | データエクスポート・インポート | — | 既に実装済み（app.js exportData/importData） |
| 3 | デモデータクリア | `c843340`, `e461e05` | clearDemoData()追加、設定画面に2ボタン追加(v298) |
| 4 | 更新時のデータ移行テスト | `9022cf0` | テスト5項目追加（C43-C47）、IndexedDB永続性確認済み |

#### QAサイクル記録
- **GitHub Pages**: Phase1 2テーマ（SW/PWA + リグレッション）→ manifest.json id修正のみ、テスト99/99×2
- **デモデータクリア**: Phase1 2テーマ（データ整合性 + UI/リグレッション）→ アトミック性修正、テスト104/104×2

#### デプロイ情報
- URL: `https://uulif.github.io/matsumura-method/`
- ソースブランチ: `大型アップデートver1.0`
- ビルドタイプ: legacy（GitHub Pages標準）

---

## 2026-03-12 セッションメモ

### 実装内容（v299→v301、4コミット）

| コミット | バージョン | 内容 |
|---|---|---|
| `f85fb34` | v299 | Googleドライブバックアップ機能追加 |
| `bf349de` | v300 | 初回ログイン画面・自動バックアップ/復元・最新状態更新ボタン追加 |
| `92267d7` | v301 | QA Phase1 Round1: Google Drive連携の15件修正 |
| `06a943e` | - | QA Phase2: Google Driveテスト15項目追加 + 既存テストwelcomeScreen対応 |

### Google Drive連携 QAサイクル記録

#### Phase 1: コードレビュー（2ラウンド、4テーマ×エキスパート）

**テーマ**: XSS/セキュリティ、データ整合性、エラーハンドリング、ロジック正当性

**Round 1 修正（15件）:**

| # | 重大度 | 問題 | 修正内容 |
|---|---|---|---|
| G1 | 致命的 | _showDriveRestoreModal XSS | innerHTML→createElement+textContent+addEventListener |
| G2 | 致命的 | _driveRequest非401エラー無視 | !res.ok時にthrow追加 |
| G3 | 致命的 | 401時にDB未更新 | saveSetting('googleDriveConnected', false)追加 |
| G4 | 重要 | _restoreData LOCAL_ONLY無視 | _LOCAL_ONLY_SETTINGS配列で端末固有設定を除外 |
| G5 | 重要 | initGoogleAuthリトライ無限 | MAX_AUTH_RETRIES=10 + 指数バックオフ |
| G6 | 重要 | _startAutoSignInポーリング無限 | MAX_RETRIES=20で抽出 |
| G7 | 重要 | signInGoogle固定1500ms待機 | 300ms×10ポーリングに変更 |
| G8 | 重要 | initGoogleAuthエラー時_welcomeLogin残存 | false代入追加 |
| G9 | 重要 | welcomeShown矛盾状態 | googleDriveConnectedチェック追加 |
| G10 | 重要 | signOutGoogleトークン→revoke順序 | ローカル変数退避→状態クリア→revoke |
| G11 | 重要 | visibilitychangeバックアップ順序 | saveCurrentPageData().then(→_autoBackupToDrive) |
| G13 | 重要 | confirmファイル名サニタイズ | replace(/[\r\n]/g,'').slice(0,50) |
| G15 | 重要 | exportDateチェックなし | !data.exportDateガード追加 |
| G16 | 重要 | 復元コード分散 | _restoreData共通メソッド化 |
| - | 軽微 | 復元モーダル二重表示 | 既存チェック追加 |

**Round 2**: 全修正確認完了、新規問題なし

**設計変更レベル（スキップ5件）:**
- 復元がmerge（put）でreplace（clearStore）ではない
- 復元にトランザクション保証なし
- 自動復元にユーザー確認なし
- トークンリフレッシュ機構なし
- タブ閉じ時のバックアップ完了保証なし（keepalive）

#### Phase 2: Playwrightテスト

**テスト項目**: D01〜D15（15項目）
| テスト | 内容 |
|---|---|
| D01 | 復元モーダルXSS防止（textContent） |
| D02 | addEventListener登録確認 |
| D03 | 非401エラーで例外 |
| D04 | ネットワークエラーメッセージ |
| D05 | 401時DB永続化 |
| D06 | LOCAL_ONLY_SETTINGS除外 |
| D07 | initGoogleAuthリトライ上限 |
| D08 | _startAutoSignIn存在 |
| D09 | welcomeShown設定存在 |
| D10 | _autoBackupToDrive async |
| D11 | _restoreFromDriveFile存在 |
| D12 | exportDateなしスキップ |
| D13 | _restoreData存在 |
| D14 | モーダル二重防止 |
| D15 | _LOCAL_ONLY_SETTINGS定義 |

**結果**: 15/15 × 2回連続pass + 全119項目 × 1回連続pass

**追加修正**: 既存テスト3ファイルのwaitForAppにwelcomeScreen dismiss処理追加（v300で追加されたウェルカム画面がテストをブロックしていた問題を解消）

---

## 2026-03-12 セッションメモ（続き：メソッド整合性改善）

### 前セッションからの継続
前セッション（同日）でv308〜v314を実装済み（Firebase連携、月次評価、週次メモ、ヘッダーバグ修正等）。
本セッションではメソッド文書との整合性改善に注力。

### ユーザーからメソッド文書を全文受領
松村メソッドの使い方ガイド全文（全体像・日誌・ルーティン5コア・6ルール・定期見直し・月次振り返りシステム・月次6ステップ・F・BOX GTD統合）を受領。アプリとの整合性確認に使用。

### 実装内容（v315→v316、2コミット）

| コミット | バージョン | 内容 |
|---|---|---|
| `0ba1c1e` | v315 | ページガイドポップアップ（月次タブのタイトルタップで説明表示）+ 「コア行動」→「期日目標」リネーム |
| `f03ace6` | v316 | 定期見直しチェックリスト（ホーム左上）+ 月次タブ上部に目標バー + 旧ガイドバナー削除 |

### v315 詳細
- PAGE_GUIDE定数（8ページ分のSTEP番号+説明文）をapp.jsに追加
- showPageGuide()メソッド追加（STEPバッジ付きポップアップ）
- renderHeaderにtitleActionオプション追加（月次ページのみヘッダータイトルにonclick付与）
- swipePages配列の「コア行動」→「期日目標」にリネーム
- 月末評価の説明文「5コア行動を見直す」→「ルーティン設計を見直す」に修正

### v316 詳細
**① 定期見直しチェックリスト**
- ホームヘッダー左上にクリップボードアイコン常時表示
- renderHeaderにleftHtmlオプション追加（backBtnをleftBtnにリファクタ）
- タップで3カテゴリのチェックリストポップアップ：
  - 毎日3項目: F・BOX処理、アクションリスト選択、カレンダー確認
  - 週次5項目: 義務/維持ルーティン確認、待機/プロジェクトリスト確認、来週分落とし込み
  - 月次4項目: いつかやりたいリスト、資料保管、候補ルーティン、月次振り返り
- トグル式チェック（IndexedDB settings 'reviewCheckState'に保存）
- 自動リセット: 毎日/毎週月曜/毎月1日
- 通知バッジ: 週末（土日）＋月末（最終5日間）
- 旧getGuideContext()メソッド削除、旧weekend/monthendガイドバナー削除

**② 月次目標バー**
- monthly-1〜7（目標タブ以外）の上部に1行バー表示
- 「目標：〇〇〇...」切り詰め表示、タップで全文展開/折りたたみ
- ▼ chevronアニメーション付き
- _goalBarExpandedプロパティ（メモリのみ、永続化なし＝意図的）
- 目標未入力時は非表示

### QAサイクル記録

**v315:**
- Phase1: 3名エキスパートレビュー → 軽微3件修正（旧名称残存、opacity調整、nowrap追加）
- Phase2: 134/134 × 2回通過

**v316:**
- Phase1: 3名エキスパートレビュー → 2件修正（review-itemにmin-height:44px、目標バーにchevronインジケータ）
- Phase2: 134/134 × 2回通過

---

## 現在の状態（次回セッション用）

**「MMメソッドの続き」と言えばここから再開。**

### インフラ整備完了
4項目全て完了。GitHub Pagesで公開中。Google Drive連携もQA完了。

### 直近の実装状況
- v366（service-worker最新）: 全てpush済み
- 最新コミット: `5754138` v366

### 次にやるべきこと（優先順）

1. **Googleカレンダー連携の実機テスト** — GCP側の設定確認（Calendar API有効化・オリジン登録）が未確認
2. **メソッド文書の更新** — ユーザーから受領した文書はアプリ変更を反映していない
3. **ヘルプテキスト追加**（~50+箇所）— 未着手
4. **日誌ページの旧式ヘルプボタン**（5箇所）変換 — 未着手

### 残りの監査項目（設計変更レベル・優先度低）

- 旧監査 #5: loadAllData再呼び出し
- 旧監査 #7: DBスキーマ変更不可
- 新監査 N11: save/load競合（排他制御）
- 新監査 N12: インポートのバッチ保存
- 新監査 N13: initDBFallbackストア不足
- Drive QA: 復元=merge（トランザクション・clearStore未対応）
- Drive QA: 自動復元に確認なし
- Drive QA: トークンリフレッシュなし
- Drive QA: タブ閉じ時バックアップ完了保証なし

### プロジェクトパス
- `C:\Users\uulif\Desktop\matsumura-method\`

### ブランチ
- `大型アップデートver1.0`（リモートより2コミット先行: v315, v316）

### 最新コミット
- `02c7fb2` QA Phase2: 内部改善テスト18件追加
- `4988247` QA Phase1 Round2: 3件の指摘を修正（v321）
- `ae237bc` QA Phase1 Round1: 13件の指摘を修正（v320）
- `4bbd40e` 内部改善9件 + ヘルプテキスト追加（v319）

### キャッシュバスター
- v366（service-worker.js）

### DB_VERSION
- 6（変更なし）

### 2025-03-15 v327-328: 日誌一覧・GTDリスト4件修正 + QAサイクル
- 修正内容:
  1. 日誌一覧に月切り替えナビ追加（← YYYY年M月 →）
  2. 日誌一覧カード全体タップで日誌に遷移（タイトル展開機能廃止）
  3. ルーティン一覧UIをタスクと統一（枠線カード＋チェックボックス＋toggleRoutineStatus追加）
  4. タスク最後のカード下枠修正（旧CSS競合解消）
- QA Phase1（4テーマ並行）:
  - 致命的2件修正: data.journals上書き問題（navigateToTodayJournal→常にDB取得、delete/save→journalListMonth使用）
  - 重要2件修正: changeJournalListMonth排他制御+エラーハンドリング
  - Round2で問題なし確認
- QA Phase2: v327-fixes.test.js 15件追加、全184テスト中181パス（3件は既存flaky、今回と無関係）
- バージョン: v328（service-worker）
- 残タスク: 振り返りにルーティンチェック表追加（DB設計変更必要、バッチ2として保留）

### 2026-03-15 v323-v326: Googleカレンダー連携

| コミット | バージョン | 内容 |
|---|---|---|
| `f45b0fc` | v323 | Googleカレンダー連携機能を実装 |
| `81ad05f` | v324 | QA Phase1 Round1: 13件修正 |
| `a24a6aa` | - | QA Phase1 Round2: 3件修正 |
| `745a5f9` | - | QA Phase2: テスト17件追加 + 終日イベント日付ずれ修正 |
| `154a5cc` | v325 | 設計レベル3件修正（ID返却・再認証・未使用ヘルプ整理） |
| `34a4912` | v326 | 全体統合QA: 致命的2件+重要2件修正 |

**機能:**
- タスク詳細画面から「Googleカレンダーに送信」ボタン
- Firebase AuthのOAuthトークンでGoogle Calendar APIを直接呼び出し
- 新規作成(POST)/更新(PUT)/404時の再作成
- gcalEventIdをタスクに保存して追跡
- 日時入力ダイアログ（日付なしタスク用）
- 401リトライ・403判定・二重送信防止

**前提条件（コード外）:**
- Google Cloud ConsoleでGoogle Calendar API有効化が必要
- OAuth同意画面の設定が必要
- OAuth 2.0クライアントIDの承認済みJavaScriptオリジンに `https://uulif.github.io` 登録が必要

### 2026-03-15 v329-v330: 振り返りルーティンチェック表

- 振り返り月詳細ページに「ルーティン」タブ追加
- 月間達成率・ルーティン別バーチャート・日別マトリクス表示
- タップでルーティンステータス切替可能
- QA修正+テスト追加完了

### 2026-03-15 v331-v334: UI修正・Notion連携・AI改善

| コミット | バージョン | 内容 |
|---|---|---|
| `fb0c4af` | v331 | UI修正7件 + Notion連携 + GitHub整理 + APIキー保護確認 |
| `88dc822` | v332 | QA修正（致命的1件+重要8件+軽微4件） |
| `eff462c` | v333 | QA Round2修正 |
| `9df3894` | v334 | Notion連携をcorsproxy.ioから自前Cloudflare Workersプロキシに切替 |

**Notion API接続情報:**
- Worker URL: https://notion-proxy.uulife98.workers.dev
- Notion API Key: 設定画面で入力（IndexedDBに保存）
- Notion Page ID: 設定画面で入力

### 2026-03-15 v335-v343: AI・振り返り・Notion改善

- AIコメントを1バージョン化 + 長文時の文量増加
- AI添削ボタンのUI統一と再添削しやすさ改善
- 振り返り一覧に長期目標サイドバー表示追加
- 振り返りにAIコメント表示+日誌編集への遷移追加
- Notionエクスポート全面修正
- QA修正（セキュリティ・エラーハンドリング・ダークモード・CSSデッドコード削除）
- GTDボタン縮小・ホーム画面統一

### 2026-03-15〜16 v344-v352: UI改善・Notion個別同期

| ver | 内容 |
|---|---|
| v344-v345 | GTD整理ボタン円形化→テキスト追加 |
| v346 | 振り返り長期目標ラベルクリックで詳細画面遷移 |
| v347-v348 | F・BOXアイテムをチェックボックス化（ワンタップ削除+undoトースト） |
| v349 | 整理ボタンを円形→コンパクト中央配置に変更 |
| v350 | クイックメモ全削除 + 日誌一覧タイトルをインライン化（達成率の右） |
| v351 | QAデッドコード254行削除（quickmemo・日誌タイトル展開関連） |
| v352 | Notion個別同期 + 一括エクスポート進捗表示 |
| v352fix | QA修正（長期目標Notion構造統一・二重読み込み解消） |

**v350 主要変更:**
- クイックメモセクション（⑥）を全日誌ページから完全削除
- 日誌一覧のタイトルを2行目→ヘッダー内インライン（達成率の右側）に移動
- 関連デッドコード削除（app.js deleteQuickMemo/undoDeleteMemo/todayMemos、CSS quickmemo-*）

**v352 Notion個別同期:**
- 日誌保存→その日の日誌+ルーティン集計をNotion同期
- 月次目標保存→月目標をNotion同期
- 長期目標保存→全長期目標をNotion同期（exportToNotionと同じブロック構造）
- 一括エクスポート→進捗表示（1/47 2025年3月 日誌...）+ DB二重読み込み解消
- 全てバックグラウンド実行（.catch(() => {})）、自動保存では発火しない

---

### 進行中の作業: UI刷新（保留中）
- 設計書: `UI刷新設計書.md`（Phase 0完了、コード調査完了、エージェントプロンプト5本記載済み）
- 手順・プロンプト・クラッシュ対策は全て設計書に記載済み。設計書を読めば実行できる

### メソッド文書
- ユーザーから全文受領済み（全体像・日誌・ルーティン5コア/6ルール・定期見直し・月次振り返り・月次6ステップ・F・BOX GTD統合）
- この文書はアプリの会話メモ.mdには含めていない（長すぎるため）。ユーザーに再度貼り付けてもらうか、別ファイル化が必要

---

## 2026-03-15 サブエージェント禁止フック設定

### 経緯
- サブエージェントに調査を委託 → 結果を検証せずユーザーに報告 → 1件が誤報と判明
- サブエージェントはブラックボックス。途中の思考過程が見えず、結論の正しさを検証できない
- トークン節約にもならない（各エージェントにオーバーヘッド＋ファイル重複読み込み）
- サブエージェントの本当のメリットは速度とコンテキスト保護だけ。信頼性と引き換えにする価値はない

### 設定内容
- `.claude/settings.json` に `PreToolUse` フックを追加
- `matcher: "Task"` で全サブエージェント呼び出しをブロック（`exit 2`）
- permissions.allowから `"Task"` を削除（自動許可も外した）
- ブロック時メッセージ：「サブエージェント禁止: 自分でコードを読んで自分で検証しろ」

### 追加ブロック（同セッション）
- `.claude/settings.json` に `WebFetch` のPreToolUseフックを追加（物理ブロック）
- `CLAUDE.md` に「検証不能ツールの禁止」セクションを追加（Task, WebFetch, Bash background の3つ）
- Task, WebFetch = フックで物理ブロック / Bash background = CLAUDE.mdで禁止命令

### 整理したファイル
- 削除: `Claude Code\プロジェクト\matsumura-method\会話メモ.md`（旧版。156行のv233止まり）
- 確認: `.claude\projects\...\memory\MEMORY.md` は元から存在しなかった
- 永続情報の保存先は **CLAUDE.md（命令）** と **会話メモ.md（メモ）** の2本に統一

---

## 2026-03-15 実使用上の問題点リスト（未検証）

**注意：サブエージェントによるコードレビューから抽出した問題リスト。自分自身でコードを追って検証していない。1件（#3の旧版「意気込みだけの日誌が消える」）は誤報と確認済み。残りも検証が必要。**

### A. iOS PWA固有の問題

| # | 問題 | 重大度 | 検証状態 |
|---|---|---|---|
| 1 | `signInWithPopup`がiOS PWAで動かない → Google連携不能 | 致命的 | 未検証 |
| 2 | WebKitの7日ポリシーでIndexedDB自動削除 → 全データ消失。`navigator.storage.persist()`未実装 | 致命的 | 未検証 |
| 3 | `100vh`がアドレスバー含む高さになる → 画面下が切れる | 重要 | 未検証 |
| 4 | `safe-area-inset-top`未設定 → ノッチにヘッダーが隠れる | 重要 | 未検証 |

### B. Android/iOS共通の問題

| # | 問題 | 重大度 | 検証状態 |
|---|---|---|---|
| 5 | DB初期化2回失敗で全データが空になる | 致命的 | 未検証 |
| 6 | ダークモードで黒背景に黒文字（ハードコード色81箇所） | 重要 | 未検証 |
| 7 | 入力中にスライダー操作→render()で全DOM再描画→フォーカス・スクロール消失 | 重要 | 未検証 |
| 8 | 縦スクロール中にスワイプ誤判定でページ遷移 | 重要 | 未検証 |
| 9 | スケジュール時刻が正時固定（分が無視される） | 重要 | 未検証 |
| 10 | 自動保存完了前にクラウド同期が走る可能性 | 重要 | 未検証 |
| 11 | クラウド復元で新しいデータが古いデータで上書きされる可能性 | 重要 | 未検証 |
| 12 | 資料の添付ファイルがクラウドバックアップに含まれない | 重要 | 未検証 |
| 13 | ノートビュー期限が深夜0-3時で1日ずれる | 軽微 | 未検証 |
| 14 | F・BOXチェックで確認なく即削除 | 軽微 | 未検証 |
| 15 | タスク完了取り消しが10秒トーストのみ | 軽微 | 未検証 |
| 16 | AI機能が毎回外部CDN取得（オフライン不可） | 軽微 | 未検証 |
| 17 | Googleカレンダートークン55分で切れ自動送信失敗 | 軽微 | 未検証 |
| 18 | 自動保存の視覚フィードバックなし | 軽微 | 未検証 |
| 19 | ルーティン△（partial）の意味説明なし | 軽微 | 未検証 |
| 20 | カレンダーアイコンが進捗画面を開く | 軽微 | 未検証 |
| 21 | スコア0と未入力が区別不能 | 軽微 | 未検証 |

### 誤報確認済み

| # | 元の主張 | 検証結果 |
|---|---|---|
| - | 意気込みだけ書いた日誌が自動削除される | **誤報**。db.js:388で`if (journal.resolution) return true;`により保存される |

---

### 2026-03-15 v354: コード品質調査 → バグ修正12件 + QA修正1件

#### 経緯
サブエージェントに丸投げした品質調査（21件報告）を自分の目で全ファイル全文読了して検証。
- 全6ファイル約27,500行を全文読了（db.js, app.js, pages.js, noteview.js, style.css, index.html）
- 21件中10件が本物、3件嘘、2件事実誤認、2件仕様通り、4件誇張
- 自分で追加5件発見

#### 修正内容（v354 コミット: 4c71126）
**致命的3件:**
- スケジュールの分(minute)が保存されない（app.js, pages.js）
- pagehide時データロスト → 30秒自動保存追加（app.js）
- signInWithPopup iOS PWA不可 → signInWithRedirectフォールバック（app.js）

**重要7件:**
- 100vh → 100dvhフォールバック（style.css 3箇所）
- viewport-fit=cover + safe-area-inset-top（index.html, style.css）
- ルーティン△を全集計で0.5カウント（db.js, app.js 5箇所, pages.js 3箇所）
- Googleカレンダー自動送信レースコンディション防止（app.js）
- トークンリフレッシュiOS PWA対応（app.js）
- scores判定修正 - キー存在で保存対象（db.js）
- 保存フィードバックトースト追加（app.js）

**軽微2件:**
- △の意味をヘルプテキストで説明（app.js）
- カレンダーアイコン遷移は正常動作と確認（修正不要）

#### QA修正（v354fix コミット: f318889）
- _autoSendToGcalの早期returnをtry内に移動しfinally確実実行を保証

#### 残存する設計変更レベルの課題（未対応）
1. innerHTML全置換 → 差分レンダリング
2. 81箇所以上のハードコードカラー → CSS変数統一
3. ワンタイムタスク/ルーティン混在 → データモデル分離
4. インポートデータスキーマ検証

---

### 2026-03-15 カレンダー機能強化 詳細設計書（v2 確定版）

**背景:**
- 右上カレンダーボタンの遷移先が実質使い道がない
- 1日の流れを月レベルでワンタップで見たいニーズ
- Googleカレンダーは「日時確定済みの予定」専用として維持

#### データ設計：新ストア不要・全て動的計算
IndexedDBスキーマは変更しない。
- スケジュール: checkPatternApplies(pattern, date) で任意日付のパターンを動的計算
- ルーティン: 過去→journal.routines / 未来→monthlyGoal.routinesテンプレート
- コアアクション: 同上
- タスク: tasksストアのdateTime/deadline照合

#### カレンダーページUI
上半分: 月グリッド（既存＋パターン/タスクマーク追加）
下半分: 日タップ→詳細パネル（タイムライン＋ルーティン○△×トグル＋コアアクション＋タスク）
未来日: テンプレート表示のみ、トグル不可

#### 「カレンダーに反映」ボタン
月次目標ページに配置。当月の既存journalに一括反映。
- 追加モード: 既存進捗保持＋新ルーティン名/コアアクション名反映
- リセットモード: 全status=none, 全done=false（確認2段階）
syncMonthlyToSpecificJournal() を新設し、syncMonthlyToJournal を委譲

#### 「過去の月からコピー」ボタン
月次目標ページ最上部。全月のmonthlyGoalを取得→新しい順一覧→選択→コピー。
コピー対象: routines, coreActions, schedulePatterns（ID振り直し）
コピーしない: goal, vision, perspectives, patterns, breakdown, reward, support

#### ルーティン○△×トグル
journal存在日のみ: none→done→partial→none サイクル
journal非存在（未来日）: テンプレート表示のみ、グレーアウト

#### 新規メソッド（app.js）
- getDateMatchingPatterns(dateObj): 任意日のパターン取得
- getCalendarDayData(dateStr): 統合日データ取得
- syncMonthlyToSpecificJournal(journal, mode): 一般化同期
- reflectMonthlyToCalendar(mode): 月全体一括反映
- copyFromPastMonth(sourceYearMonth): 過去月コピー
- showCopyMonthModal() / showReflectModal(): モーダル表示
- toggleCalendarRoutine(dateStr, idx): カレンダールーティントグル

#### 変更関数（pages.js）
- renderCalendarPage: 大幅書き直し
- renderReviewCalendar: セルにパターン/タスクマーク追加
- renderCalendarDayDetail: 新規（詳細パネル）
- renderMonthlyGoalSection: コピーボタン追加
- renderMonthlyRoutineSection: 反映ボタン追加

#### 注意点
- 月切替時: loadReviewCalendarJournalsにmonthlyGoal読み込み追加必要
- タスク: カレンダーページ初期化時にtasksストア読み込み追加
- パフォーマンス: 31日×5パターン=155回のマッチング→問題なし
- innerHTML: 現行のrender()全置換のまま。トグル時はスクロール位置保存で対処

#### 実装順序
1. getDateMatchingPatterns + syncMonthlyToSpecificJournal（リファクタ）
2. reflectMonthlyToCalendar + UI
3. copyFromPastMonth + UI
4. カレンダーページUI刷新（月グリッド強化＋詳細パネル）
5. toggleCalendarRoutine
6. タスク表示統合
7. ダークモード対応 + CSS仕上げ
8. QAサイクル

---

## 2026-03-18 セッションメモ

### 実装内容（v366、コミット: `5754138`）

| 変更 | 内容 |
|---|---|
| 逆算目標の日付自動計算 | `addMilestone()`を修正。目標期限の1ヶ月前を自動セット。追加するたびに1ヶ月ずつ遡る。来月（今月+1）まで到達したら追加ボタン非表示 |
| APIキー保護 | `_LOCAL_ONLY_SETTINGS`に`geminiApiKey`、`notionApiKey`、`notionPageId`を追加。クラウド復元でAPIキーが上書き消去されなくなった |
| 更新ボタン修正 | `checkForUpdate()`をSW解除+キャッシュ全削除+リロードに書き換え。旧コードは`skipWaiting()`完了後のチェックが間に合わず常に「最新です」と表示されていた |

### 今回のセッションで起きた問題と教訓

#### 問題1: 間違ったフォルダで作業した
- `C:\Users\uulif\Desktop\Claude Code\プロジェクト\matsumura-method`（古いgitコピー）を本番だと勘違い
- 実際の本番は `C:\Users\uulif\Desktop\matsumura-method`
- 原因: セッション開始時にデプロイ先を確認しなかった
- **教訓: セッション開始時に必ず会話メモ.mdとデプロイ設定を読む**

#### 問題2: Netlifyに無断デプロイした
- 本番がNetlifyだと勘違いし、netlify-cliをインストールしてデプロイ実行
- 実際の本番はGitHub Pages
- Netlifyサイトは削除済み（サイトID: 918311ed-fe0b-45cf-86a3-dc591d8d40c7）
- .netlifyフォルダは手動削除が必要
- **教訓: デプロイ操作は必ずユーザーに確認してから実行する**

#### 問題3: APIキーがクラウド復元で消えていた
- `geminiApiKey`と`notionApiKey`が`_LOCAL_ONLY_SETTINGS`に含まれていなかった
- クラウド復元時に空値で上書きされていた
- 修正済み: 3キーをLOCAL_ONLYに追加

### 間違ったフォルダのクリーンアップ
- `Claude Code\プロジェクト\matsumura-method`の不要コミット3件を`git reset --hard 2c9d384`で巻き戻し済み
- netlify-cliはアンインストール済み
